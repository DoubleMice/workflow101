Generate or update documentation for $ARGUMENTS (default: the current project).

1. Read the source code to understand the public API
2. Check for existing docs (README, docstrings, comments)
3. Generate documentation covering:
   - Module/function purpose
   - Parameters and return values
   - Usage examples
4. If docs already exist, update them to match current code
5. Output as markdown
